interface PersonajeLOR{
    nombre : string,
    pv: number;
    mostrarHp:()=>void;
}

function sumar(a:number,b: number) : number{
    return (a + b);
}

const sumarFlecha = (a:number,b:number):number=>{
    return a + b;
}

function multiplicar(numero : number,otroNumero?: number,base: number=2){
    return numero * base;
}
 const resultado = sumar(10,10 );
  
console.log(resultado);

const resultadoo = multiplicar(10,20,20)

//siempre colocar el tipo a  los argumentos 
function cura0() : void{
    return undefined
}

function curar(PersonajeLOR, curarX):void{

PersonajeLOR.pv += curarX;
console.log(nuevoPersonaje);
}
//siempre usar constantes 
//este es un objeto literal entonces devo usar todas las firmas de las interfases
const nuevoPersonaje: PersonajeLOR = {
  nombre:'ale',
  pv:100,
  mostrarHp(){
    console.log('Puntos de vida', this.pv);
  }
}

curar(nuevoPersonaje,20)